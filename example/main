#!/usr/bin/env -S python3 -B

# expose source directory (if not installed)
import importlib.util
if not importlib.util.find_spec("pycli"):
    import sys
    sys.path.append("..")

# example invocation
import pycli
if __name__ == "__main__":
    pycli.run("cli")
